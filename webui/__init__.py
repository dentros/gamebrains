"""
GameBrains web UI — a temporary/demo interface, not the final "houses" front-end from
CLAUDE.md's architecture diagram. Pick every parameter in a form, run a real match through the
same engine/agents/metrics/repository code the CLI (`experiments/run_pgg.py`) uses, and see the
results (creatures, cooperation chart, full console log, repository/Smart-Filter/equilibrium/
Φ-autonomy panels) rendered in one page. Local-only (Flask dev server), meant for screen-sharing a
live demo, not for deployment.
"""
